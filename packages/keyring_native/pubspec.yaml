name: keyring_native
version: 1.0.0
description: Native keyring implementation using Rust via native_toolchain_rust

resolution: workspace
environment:
  sdk: ^3.12.1

dependencies:
  keyring_core: any
  native_toolchain_rust: ^1.0.4+0
  hooks: ^2.0.2
  logging: ^1.3.0
  ffi: ^2.1.4

dev_dependencies:
  ffigen: ^20.1.0
  lints: ^6.0.0
  test: ^1.25.6
