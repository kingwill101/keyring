## 1.0.0

- Initial release.
- Command-line interface with 8 subcommands: `info`, `set`, `password`,
  `secret`, `attributes`, `credential`, `delete`, `search`.
- Backend selection via `--module` flag.
- Built on `package:artisanal` for argument parsing and console IO.
