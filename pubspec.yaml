name: keyring_workspace
description: Workspace for Dart keyring package ecosystem
version: 1.0.0
publish_to: none

environment:
  sdk: ^3.12.1

workspace:
  - packages/*

dependencies:
  artisanal: ^0.3.0
  hooks: ^2.0.2
  logging: ^1.3.0
  native_toolchain_rust: ^1.0.4+0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
